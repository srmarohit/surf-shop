
<div style="margin:20px 0">
<% if(posts.page-1) { %>
  <a href="/posts?page=<%= posts.page - 1 %>">prev</a>
<% } %>

<% for(let i =1;i<=posts.pages;i++){ %>
     <a href="/posts?page=<%= i %>" 
     	<%= posts.page === i ? 'style=color:#000;':''  %>
     	>
     	<%= i %>
     	</a>
<% } %>

<% if((posts.page + 1) <= posts.pages) { %>
  <a href="/posts?page=<%= posts.page + 1 %>">next</a>
<% } %>

</div>